# Backend base URL used by the frontend in local development.
# The OAuth2 redirect must go directly to the backend (not the Vite dev server)
# because window.location.href navigation bypasses the Vite proxy.
VITE_API_BASE_URL=http://localhost:8080
